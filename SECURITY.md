# Security policy

Do not report suspected vulnerabilities through public issues before a private reporting channel is approved.

Until the public source repository and security contact are launched, contact the project owner directly. Do not include credentials, personal student data, private `.fls` projects, or exploit payloads in an initial message.

The static website has no application backend. A future contact service must be maintained and reviewed as a separate security boundary.
