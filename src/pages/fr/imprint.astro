---
import BaseLayout from '../../layouts/BaseLayout.astro';
import TrustPage from '../../components/TrustPage.astro';
import { copy } from '../../lib/content';
const locale = 'fr' as const;
const c = copy[locale];
---
<BaseLayout locale={locale} title={`${c.imprint.title} – Filius on iPad`} description={c.imprint.intro} noindex>
  <TrustPage locale={locale} kind="imprint" />
</BaseLayout>
